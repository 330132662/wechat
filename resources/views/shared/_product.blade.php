<a id="ok" class="col-xs-12">
    <div><strong><a href="{{ url('products/'.$article->id) }}">{{ $article->name }}</a></strong></div>
    <div>author {{ $article->price }}</div>
    <div>in {{ $article->appname }}</div>
</a>
